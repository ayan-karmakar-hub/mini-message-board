extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
  - for (var i = 0; i < messages.length; ++i)
      li= `"${messages[i].text}" - ${messages[i].user}`
  a(href="/new") Add message